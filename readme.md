# Causal Discovery with Causal-Copilot
## installation conflicts handle

install in order to recover numpy version  
pip install -r requirements_cpu.txt
pip install causalnex==0.12.1  
pip install numpy==1.26.4  